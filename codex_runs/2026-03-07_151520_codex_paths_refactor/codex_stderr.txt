OpenAI Codex v0.111.0 (research preview)
--------
workdir: /home/resis/src/polysyndicate-v2
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR, /home/resis/.codex/memories]
reasoning effort: none
reasoning summaries: none
session id: 019cc8a7-1f24-78e3-9a58-93608711c3e9
--------
user
TASK: Align codex runner with new /codex directory structure.

CONTEXT:
Repository contains automation scripts:
- scripts/codex_runner/run_codex_task.sh
- scripts/codex_runner/collect_artifacts.sh
- docs/codex_runner.md

We migrated to new structure:

  codex/
    prompts/      ← prompt files
    runs/         ← run outputs
    templates/    ← prompt templates

GOAL:
Adjust runner + docs so that:

1) Default prompt path examples point to codex/prompts/
2) Default run output directory becomes codex/runs/ instead of codex_runs/
3) All docs reflect the new structure
4) Backward compatibility preserved if someone still uses old paths

CONSTRAINTS:
- Do not change runner logic
- Do not change codex CLI invocation
- Modify only path defaults and documentation
- Keep scripts POSIX-compatible

ACCEPTANCE:
- Running existing commands still works
- New runs land in codex/runs/
- Docs show correct tree
- Patch minimal and clean

OUTPUT:
Normal code patch + updated docs. No extra explanations.
mcp startup: no servers
codex
I’m updating the codex runner defaults and docs to use the new `codex/` layout while keeping legacy paths working. I’ll inspect the two scripts and the runner docs first, then apply a minimal patch limited to path defaults and examples.
exec
/bin/bash -lc "sed -n '1,220p' scripts/codex_runner/run_codex_task.sh" in /home/resis/src/polysyndicate-v2exec
/bin/bash -lc "sed -n '1,260p' docs/codex_runner.md" in /home/resis/src/polysyndicate-v2exec
/bin/bash -lc "sed -n '1,220p' scripts/codex_runner/collect_artifacts.sh" in /home/resis/src/polysyndicate-v2 succeeded in 51ms:
#!/usr/bin/env bash
set -euo pipefail

usage() {
  cat <<USAGE
Usage: $0 [--run-tests] [--py-compile] [--name <run-name>] [--dry-run] <prompt-file>
USAGE
}

RUN_TESTS=0
RUN_PY_COMPILE=0
DRY_RUN=0
RUN_NAME=""
PROMPT_FILE=""

while [[ $# -gt 0 ]]; do
  case "$1" in
    --run-tests)
      RUN_TESTS=1
      shift
 succeeded in 50ms:
#!/usr/bin/env bash
set -euo pipefail

usage() {
  cat <<USAGE
Usage: $0 --run-dir <path> [--run-tests] [--py-compile]
USAGE
}

RUN_DIR=""
RUN_TESTS=0
RUN_PY_COMPILE=0

while [[ $# -gt 0 ]]; do
  case "$1" in
    --run-dir)
      RUN_DIR="${2:-}"
      shift 2
      ;;
    --run-tests)
 succeeded in 50ms:
# Codex Runner (Local)

Минимальный локальный набор для запуска задач Codex из файла без ручной оркестрации.

## Быстрый старт
1. Сохраните задачу в файл, например `prompts/my_task.txt`.
2. Запустите:

```bash
./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
```

## Поддерживаемые команды

```bash
./scripts/codex_runner/run_codex_task.sh [--run-tests] [--py-compile] [--name <run-name>] [--dry-run] <prompt-file>
```

Опции:
- `--run-tests`: после работы Codex запускает `python -m pytest -q` и сохраняет вывод.
codex
I found hardcoded defaults in `run_codex_task.sh` and old examples in `docs/codex_runner.md`. I’ll do a quick repo-wide path reference scan to update only the relevant docs while preserving compatibility behavior.
exec
/bin/bash -lc 'rg -n "codex_runs|prompts/|codex/prompts|codex/runs|codex/templates" -S' in /home/resis/src/polysyndicate-v2 succeeded in 51ms:
scripts/codex_runner/run_codex_task.sh:75:RUN_DIR="$REPO_ROOT/codex_runs/${ts}_${safe_name}"
codex/prompts/next_task.txt:12:    prompts/      ← prompt files
codex/prompts/next_task.txt:19:1) Default prompt path examples point to codex/prompts/
codex/prompts/next_task.txt:20:2) Default run output directory becomes codex/runs/ instead of codex_runs/
codex/prompts/next_task.txt:32:- New runs land in codex/runs/
codex_runs/2026-03-07_151520_codex_paths_refactor/prompt.txt:12:    prompts/      ← prompt files
codex_runs/2026-03-07_151520_codex_paths_refactor/prompt.txt:19:1) Default prompt path examples point to codex/prompts/
codex_runs/2026-03-07_151520_codex_paths_refactor/prompt.txt:20:2) Default run output directory becomes codex/runs/ instead of codex_runs/
codex_runs/2026-03-07_151520_codex_paths_refactor/prompt.txt:32:- New runs land in codex/runs/
codex_runs/2026-03-07_151520_codex_paths_refactor/codex_stderr.txt:24:    prompts/      ← prompt files
codex_runs/2026-03-07_151520_codex_paths_refactor/codex_stderr.txt:31:1) Default prompt path examples point to codex/prompts/
codex_runs/2026-03-07_151520_codex_paths_refactor/codex_stderr.txt:32:2) Default run output directory becomes codex/runs/ instead of codex_runs/
codex_runs/2026-03-07_151520_codex_paths_refactor/codex_stderr.txt:44:- New runs land in codex/runs/
codex_runs/2026-03-07_151520_codex_paths_refactor/codex_stderr.txt:104:1. Сохраните задачу в файл, например `prompts/my_task.txt`.
codex_runs/2026-03-07_151520_codex_paths_refactor/codex_stderr.txt:108:./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
docs/codex_runner.md:6:1. Сохраните задачу в файл, например `prompts/my_task.txt`.
docs/codex_runner.md:10:./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
docs/codex_runner.md:22:- `--name <run-name>`: имя прогона в каталоге `codex_runs/`.
docs/codex_runner.md:40:codex_runs/YYYY-MM-DD_HHMMSS_<name>/
codex
I’m applying a minimal patch now: switch the runner’s default output base to `codex/runs/` and update the runner docs/examples/tree to `codex/prompts` + `codex/runs`, with a brief note that legacy paths still work.
file update
M /home/resis/src/polysyndicate-v2/docs/codex_runner.md
@@ -5,3 +5,3 @@
 ## Быстрый старт
-1. Сохраните задачу в файл, например `prompts/my_task.txt`.
+1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
 2. Запустите:
@@ -9,3 +9,3 @@
 ```bash
-./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
+./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
 ```
@@ -21,3 +21,3 @@
 - `--py-compile`: запускает `python -m py_compile` по изменённым Python-файлам.
-- `--name <run-name>`: имя прогона в каталоге `codex_runs/`.
+- `--name <run-name>`: имя прогона в каталоге `codex/runs/`.
 - `--dry-run`: создаёт run-dir и артефакты без реального вызова Codex.
@@ -39,5 +39,10 @@
 ```text
-codex_runs/YYYY-MM-DD_HHMMSS_<name>/
+codex/
+  prompts/
+  runs/YYYY-MM-DD_HHMMSS_<name>/
+  templates/
 ```
 
+Можно по-прежнему использовать старые пути (`prompts/...`, `codex_runs/...`) вручную, но дефолтные примеры и новые запуски ориентированы на структуру `codex/...`.
+
 Типичный состав:
file update
M /home/resis/src/polysyndicate-v2/scripts/codex_runner/run_codex_task.sh
@@ -74,3 +74,3 @@
 ts="$(date +%Y-%m-%d_%H%M%S)"
-RUN_DIR="$REPO_ROOT/codex_runs/${ts}_${safe_name}"
+RUN_DIR="$REPO_ROOT/codex/runs/${ts}_${safe_name}"
 mkdir -p "$RUN_DIR"
apply_patch(auto_approved=true) exited 0 in 9ms:
Success. Updated the following files:
M docs/codex_runner.md
apply_patch(auto_approved=true) exited 0 in 6ms:
Success. Updated the following files:
M scripts/codex_runner/run_codex_task.sh
file update:
diff --git a/docs/codex_runner.md b/docs/codex_runner.md
index 73dd4c9c8d4afbb8a50ee28753ffb0fe34a7f874..bf200d8caa6a0963e39a90616d52eb47ebd22aad
--- a/docs/codex_runner.md
+++ b/docs/codex_runner.md
@@ -3,11 +3,11 @@
 Минимальный локальный набор для запуска задач Codex из файла без ручной оркестрации.
 
 ## Быстрый старт
-1. Сохраните задачу в файл, например `prompts/my_task.txt`.
+1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
 2. Запустите:
 
 ```bash
-./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
+./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
 ```
 
 ## Поддерживаемые команды
@@ -19,7 +19,7 @@
 Опции:
 - `--run-tests`: после работы Codex запускает `python -m pytest -q` и сохраняет вывод.
 - `--py-compile`: запускает `python -m py_compile` по изменённым Python-файлам.
-- `--name <run-name>`: имя прогона в каталоге `codex_runs/`.
+- `--name <run-name>`: имя прогона в каталоге `codex/runs/`.
 - `--dry-run`: создаёт run-dir и артефакты без реального вызова Codex.
 
 ## Неинтерактивный запуск Codex
@@ -37,9 +37,14 @@
 Для каждого запуска создаётся каталог:
 
 ```text
-codex_runs/YYYY-MM-DD_HHMMSS_<name>/
+codex/
+  prompts/
+  runs/YYYY-MM-DD_HHMMSS_<name>/
+  templates/
 ```
 
+Можно по-прежнему использовать старые пути (`prompts/...`, `codex_runs/...`) вручную, но дефолтные примеры и новые запуски ориентированы на структуру `codex/...`.
+
 Типичный состав:
 - `prompt.txt`
 - `summary.txt`

file update:
diff --git a/docs/codex_runner.md b/docs/codex_runner.md
index 73dd4c9c8d4afbb8a50ee28753ffb0fe34a7f874..bf200d8caa6a0963e39a90616d52eb47ebd22aad
--- a/docs/codex_runner.md
+++ b/docs/codex_runner.md
@@ -3,11 +3,11 @@
 Минимальный локальный набор для запуска задач Codex из файла без ручной оркестрации.
 
 ## Быстрый старт
-1. Сохраните задачу в файл, например `prompts/my_task.txt`.
+1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
 2. Запустите:
 
 ```bash
-./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
+./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
 ```
 
 ## Поддерживаемые команды
@@ -19,7 +19,7 @@
 Опции:
 - `--run-tests`: после работы Codex запускает `python -m pytest -q` и сохраняет вывод.
 - `--py-compile`: запускает `python -m py_compile` по изменённым Python-файлам.
-- `--name <run-name>`: имя прогона в каталоге `codex_runs/`.
+- `--name <run-name>`: имя прогона в каталоге `codex/runs/`.
 - `--dry-run`: создаёт run-dir и артефакты без реального вызова Codex.
 
 ## Неинтерактивный запуск Codex
@@ -37,9 +37,14 @@
 Для каждого запуска создаётся каталог:
 
 ```text
-codex_runs/YYYY-MM-DD_HHMMSS_<name>/
+codex/
+  prompts/
+  runs/YYYY-MM-DD_HHMMSS_<name>/
+  templates/
 ```
 
+Можно по-прежнему использовать старые пути (`prompts/...`, `codex_runs/...`) вручную, но дефолтные примеры и новые запуски ориентированы на структуру `codex/...`.
+
 Типичный состав:
 - `prompt.txt`
 - `summary.txt`
diff --git a/scripts/codex_runner/run_codex_task.sh b/scripts/codex_runner/run_codex_task.sh
index 5adaf1b1e685676ea7d89267afa7004229b4d6ff..4dbb8dac4f7b75eec7c51cd5e010123aa7ce9ec8
--- a/scripts/codex_runner/run_codex_task.sh
+++ b/scripts/codex_runner/run_codex_task.sh
@@ -72,7 +72,7 @@
 cd "$REPO_ROOT"
 
 ts="$(date +%Y-%m-%d_%H%M%S)"
-RUN_DIR="$REPO_ROOT/codex_runs/${ts}_${safe_name}"
+RUN_DIR="$REPO_ROOT/codex/runs/${ts}_${safe_name}"
 mkdir -p "$RUN_DIR"
 
 cp "$PROMPT_FILE" "$RUN_DIR/prompt.txt"

file update:
diff --git a/docs/codex_runner.md b/docs/codex_runner.md
index 73dd4c9c8d4afbb8a50ee28753ffb0fe34a7f874..bf200d8caa6a0963e39a90616d52eb47ebd22aad
--- a/docs/codex_runner.md
+++ b/docs/codex_runner.md
@@ -3,11 +3,11 @@
 Минимальный локальный набор для запуска задач Codex из файла без ручной оркестрации.
 
 ## Быстрый старт
-1. Сохраните задачу в файл, например `prompts/my_task.txt`.
+1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
 2. Запустите:
 
 ```bash
-./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
+./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
 ```
 
 ## Поддерживаемые команды
@@ -19,7 +19,7 @@
 Опции:
 - `--run-tests`: после работы Codex запускает `python -m pytest -q` и сохраняет вывод.
 - `--py-compile`: запускает `python -m py_compile` по изменённым Python-файлам.
-- `--name <run-name>`: имя прогона в каталоге `codex_runs/`.
+- `--name <run-name>`: имя прогона в каталоге `codex/runs/`.
 - `--dry-run`: создаёт run-dir и артефакты без реального вызова Codex.
 
 ## Неинтерактивный запуск Codex
@@ -37,9 +37,14 @@
 Для каждого запуска создаётся каталог:
 
 ```text
-codex_runs/YYYY-MM-DD_HHMMSS_<name>/
+codex/
+  prompts/
+  runs/YYYY-MM-DD_HHMMSS_<name>/
+  templates/
 ```
 
+Можно по-прежнему использовать старые пути (`prompts/...`, `codex_runs/...`) вручную, но дефолтные примеры и новые запуски ориентированы на структуру `codex/...`.
+
 Типичный состав:
 - `prompt.txt`
 - `summary.txt`
diff --git a/scripts/codex_runner/run_codex_task.sh b/scripts/codex_runner/run_codex_task.sh
index 5adaf1b1e685676ea7d89267afa7004229b4d6ff..4dbb8dac4f7b75eec7c51cd5e010123aa7ce9ec8
--- a/scripts/codex_runner/run_codex_task.sh
+++ b/scripts/codex_runner/run_codex_task.sh
@@ -72,7 +72,7 @@
 cd "$REPO_ROOT"
 
 ts="$(date +%Y-%m-%d_%H%M%S)"
-RUN_DIR="$REPO_ROOT/codex_runs/${ts}_${safe_name}"
+RUN_DIR="$REPO_ROOT/codex/runs/${ts}_${safe_name}"
 mkdir -p "$RUN_DIR"
 
 cp "$PROMPT_FILE" "$RUN_DIR/prompt.txt"

codex
I’ve made the two targeted edits and will now verify the exact diffs and scan for leftover `codex_runs` references in the runner docs/scripts.
exec
/bin/bash -lc 'git diff -- scripts/codex_runner/run_codex_task.sh docs/codex_runner.md' in /home/resis/src/polysyndicate-v2exec
/bin/bash -lc 'rg -n "codex_runs|prompts/my_task.txt|codex/prompts/my_task.txt|codex/runs" docs/codex_runner.md scripts/codex_runner/run_codex_task.sh -S' in /home/resis/src/polysyndicate-v2 succeeded in 51ms:

 succeeded in 51ms:
scripts/codex_runner/run_codex_task.sh:75:RUN_DIR="$REPO_ROOT/codex/runs/${ts}_${safe_name}"
docs/codex_runner.md:6:1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
docs/codex_runner.md:10:./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
docs/codex_runner.md:22:- `--name <run-name>`: имя прогона в каталоге `codex/runs/`.
docs/codex_runner.md:46:Можно по-прежнему использовать старые пути (`prompts/...`, `codex_runs/...`) вручную, но дефолтные примеры и новые запуски ориентированы на структуру `codex/...`.
file update:
diff --git a/docs/codex_runner.md b/docs/codex_runner.md
index 73dd4c9c8d4afbb8a50ee28753ffb0fe34a7f874..bf200d8caa6a0963e39a90616d52eb47ebd22aad
--- a/docs/codex_runner.md
+++ b/docs/codex_runner.md
@@ -3,11 +3,11 @@
 Минимальный локальный набор для запуска задач Codex из файла без ручной оркестрации.
 
 ## Быстрый старт
-1. Сохраните задачу в файл, например `prompts/my_task.txt`.
+1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
 2. Запустите:
 
 ```bash
-./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
+./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
 ```
 
 ## Поддерживаемые команды
@@ -19,7 +19,7 @@
 Опции:
 - `--run-tests`: после работы Codex запускает `python -m pytest -q` и сохраняет вывод.
 - `--py-compile`: запускает `python -m py_compile` по изменённым Python-файлам.
-- `--name <run-name>`: имя прогона в каталоге `codex_runs/`.
+- `--name <run-name>`: имя прогона в каталоге `codex/runs/`.
 - `--dry-run`: создаёт run-dir и артефакты без реального вызова Codex.
 
 ## Неинтерактивный запуск Codex
@@ -37,9 +37,14 @@
 Для каждого запуска создаётся каталог:
 
 ```text
-codex_runs/YYYY-MM-DD_HHMMSS_<name>/
+codex/
+  prompts/
+  runs/YYYY-MM-DD_HHMMSS_<name>/
+  templates/
 ```
 
+Можно по-прежнему использовать старые пути (`prompts/...`, `codex_runs/...`) вручную, но дефолтные примеры и новые запуски ориентированы на структуру `codex/...`.
+
 Типичный состав:
 - `prompt.txt`
 - `summary.txt`
diff --git a/scripts/codex_runner/run_codex_task.sh b/scripts/codex_runner/run_codex_task.sh
index 5adaf1b1e685676ea7d89267afa7004229b4d6ff..4dbb8dac4f7b75eec7c51cd5e010123aa7ce9ec8
--- a/scripts/codex_runner/run_codex_task.sh
+++ b/scripts/codex_runner/run_codex_task.sh
@@ -72,7 +72,7 @@
 cd "$REPO_ROOT"
 
 ts="$(date +%Y-%m-%d_%H%M%S)"
-RUN_DIR="$REPO_ROOT/codex_runs/${ts}_${safe_name}"
+RUN_DIR="$REPO_ROOT/codex/runs/${ts}_${safe_name}"
 mkdir -p "$RUN_DIR"
 
 cp "$PROMPT_FILE" "$RUN_DIR/prompt.txt"

exec
/bin/bash -lc "git status --short scripts/codex_runner/run_codex_task.sh docs/codex_runner.md && echo '---' && sed -n '1,120p' docs/codex_runner.md && echo '---' && sed -n '65,90p' scripts/codex_runner/run_codex_task.sh" in /home/resis/src/polysyndicate-v2 succeeded in 51ms:
?? docs/codex_runner.md
?? scripts/codex_runner/run_codex_task.sh
---
# Codex Runner (Local)

Минимальный локальный набор для запуска задач Codex из файла без ручной оркестрации.

## Быстрый старт
1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
2. Запустите:

```bash
./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
```

## Поддерживаемые команды

```bash
./scripts/codex_runner/run_codex_task.sh [--run-tests] [--py-compile] [--name <run-name>] [--dry-run] <prompt-file>
```
file update:
diff --git a/docs/codex_runner.md b/docs/codex_runner.md
index 73dd4c9c8d4afbb8a50ee28753ffb0fe34a7f874..bf200d8caa6a0963e39a90616d52eb47ebd22aad
--- a/docs/codex_runner.md
+++ b/docs/codex_runner.md
@@ -3,11 +3,11 @@
 Минимальный локальный набор для запуска задач Codex из файла без ручной оркестрации.
 
 ## Быстрый старт
-1. Сохраните задачу в файл, например `prompts/my_task.txt`.
+1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
 2. Запустите:
 
 ```bash
-./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
+./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
 ```
 
 ## Поддерживаемые команды
@@ -19,7 +19,7 @@
 Опции:
 - `--run-tests`: после работы Codex запускает `python -m pytest -q` и сохраняет вывод.
 - `--py-compile`: запускает `python -m py_compile` по изменённым Python-файлам.
-- `--name <run-name>`: имя прогона в каталоге `codex_runs/`.
+- `--name <run-name>`: имя прогона в каталоге `codex/runs/`.
 - `--dry-run`: создаёт run-dir и артефакты без реального вызова Codex.
 
 ## Неинтерактивный запуск Codex
@@ -37,9 +37,14 @@
 Для каждого запуска создаётся каталог:
 
 ```text
-codex_runs/YYYY-MM-DD_HHMMSS_<name>/
+codex/
+  prompts/
+  runs/YYYY-MM-DD_HHMMSS_<name>/
+  templates/
 ```
 
+Можно по-прежнему использовать старые пути (`prompts/...`, `codex_runs/...`) вручную, но дефолтные примеры и новые запуски ориентированы на структуру `codex/...`.
+
 Типичный состав:
 - `prompt.txt`
 - `summary.txt`
diff --git a/scripts/codex_runner/run_codex_task.sh b/scripts/codex_runner/run_codex_task.sh
index 5adaf1b1e685676ea7d89267afa7004229b4d6ff..4dbb8dac4f7b75eec7c51cd5e010123aa7ce9ec8
--- a/scripts/codex_runner/run_codex_task.sh
+++ b/scripts/codex_runner/run_codex_task.sh
@@ -72,7 +72,7 @@
 cd "$REPO_ROOT"
 
 ts="$(date +%Y-%m-%d_%H%M%S)"
-RUN_DIR="$REPO_ROOT/codex_runs/${ts}_${safe_name}"
+RUN_DIR="$REPO_ROOT/codex/runs/${ts}_${safe_name}"
 mkdir -p "$RUN_DIR"
 
 cp "$PROMPT_FILE" "$RUN_DIR/prompt.txt"

exec
/bin/bash -lc 'bash -n scripts/codex_runner/run_codex_task.sh scripts/codex_runner/collect_artifacts.sh && echo OK' in /home/resis/src/polysyndicate-v2 succeeded in 51ms:
OK
file update:
diff --git a/docs/codex_runner.md b/docs/codex_runner.md
index 73dd4c9c8d4afbb8a50ee28753ffb0fe34a7f874..bf200d8caa6a0963e39a90616d52eb47ebd22aad
--- a/docs/codex_runner.md
+++ b/docs/codex_runner.md
@@ -3,11 +3,11 @@
 Минимальный локальный набор для запуска задач Codex из файла без ручной оркестрации.
 
 ## Быстрый старт
-1. Сохраните задачу в файл, например `prompts/my_task.txt`.
+1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
 2. Запустите:
 
 ```bash
-./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
+./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
 ```
 
 ## Поддерживаемые команды
@@ -19,7 +19,7 @@
 Опции:
 - `--run-tests`: после работы Codex запускает `python -m pytest -q` и сохраняет вывод.
 - `--py-compile`: запускает `python -m py_compile` по изменённым Python-файлам.
-- `--name <run-name>`: имя прогона в каталоге `codex_runs/`.
+- `--name <run-name>`: имя прогона в каталоге `codex/runs/`.
 - `--dry-run`: создаёт run-dir и артефакты без реального вызова Codex.
 
 ## Неинтерактивный запуск Codex
@@ -37,9 +37,14 @@
 Для каждого запуска создаётся каталог:
 
 ```text
-codex_runs/YYYY-MM-DD_HHMMSS_<name>/
+codex/
+  prompts/
+  runs/YYYY-MM-DD_HHMMSS_<name>/
+  templates/
 ```
 
+Можно по-прежнему использовать старые пути (`prompts/...`, `codex_runs/...`) вручную, но дефолтные примеры и новые запуски ориентированы на структуру `codex/...`.
+
 Типичный состав:
 - `prompt.txt`
 - `summary.txt`
diff --git a/scripts/codex_runner/run_codex_task.sh b/scripts/codex_runner/run_codex_task.sh
index 5adaf1b1e685676ea7d89267afa7004229b4d6ff..4dbb8dac4f7b75eec7c51cd5e010123aa7ce9ec8
--- a/scripts/codex_runner/run_codex_task.sh
+++ b/scripts/codex_runner/run_codex_task.sh
@@ -72,7 +72,7 @@
 cd "$REPO_ROOT"
 
 ts="$(date +%Y-%m-%d_%H%M%S)"
-RUN_DIR="$REPO_ROOT/codex_runs/${ts}_${safe_name}"
+RUN_DIR="$REPO_ROOT/codex/runs/${ts}_${safe_name}"
 mkdir -p "$RUN_DIR"
 
 cp "$PROMPT_FILE" "$RUN_DIR/prompt.txt"

codex
Updated.

- [scripts/codex_runner/run_codex_task.sh](/home/resis/src/polysyndicate-v2/scripts/codex_runner/run_codex_task.sh):75  
  `RUN_DIR` default changed from `codex_runs/...` to `codex/runs/...`.

- [docs/codex_runner.md](/home/resis/src/polysyndicate-v2/docs/codex_runner.md)  
  - Quick start prompt example updated to `codex/prompts/my_task.txt`.  
  - Example command updated to `./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt`.  
  - `--name` option text updated to `codex/runs/`.  
  - Run directory structure updated to:
    - `codex/prompts/`
    - `codex/runs/YYYY-MM-DD_HHMMSS_<name>/`
    - `codex/templates/`
  - Backward-compat note added for manual legacy paths (`prompts/...`, `codex_runs/...`).
file update:
diff --git a/docs/codex_runner.md b/docs/codex_runner.md
index 73dd4c9c8d4afbb8a50ee28753ffb0fe34a7f874..bf200d8caa6a0963e39a90616d52eb47ebd22aad
--- a/docs/codex_runner.md
+++ b/docs/codex_runner.md
@@ -3,11 +3,11 @@
 Минимальный локальный набор для запуска задач Codex из файла без ручной оркестрации.
 
 ## Быстрый старт
-1. Сохраните задачу в файл, например `prompts/my_task.txt`.
+1. Сохраните задачу в файл, например `codex/prompts/my_task.txt`.
 2. Запустите:
 
 ```bash
-./scripts/codex_runner/run_codex_task.sh prompts/my_task.txt
+./scripts/codex_runner/run_codex_task.sh codex/prompts/my_task.txt
 ```
 
 ## Поддерживаемые команды
@@ -19,7 +19,7 @@
 Опции:
 - `--run-tests`: после работы Codex запускает `python -m pytest -q` и сохраняет вывод.
 - `--py-compile`: запускает `python -m py_compile` по изменённым Python-файлам.
-- `--name <run-name>`: имя прогона в каталоге `codex_runs/`.
+- `--name <run-name>`: имя прогона в каталоге `codex/runs/`.
 - `--dry-run`: создаёт run-dir и артефакты без реального вызова Codex.
 
 ## Неинтерактивный запуск Codex
@@ -37,9 +37,14 @@
 Для каждого запуска создаётся каталог:
 
 ```text
-codex_runs/YYYY-MM-DD_HHMMSS_<name>/
+codex/
+  prompts/
+  runs/YYYY-MM-DD_HHMMSS_<name>/
+  templates/
 ```
 
+Можно по-прежнему использовать старые пути (`prompts/...`, `codex_runs/...`) вручную, но дефолтные примеры и новые запуски ориентированы на структуру `codex/...`.
+
 Типичный состав:
 - `prompt.txt`
 - `summary.txt`
diff --git a/scripts/codex_runner/run_codex_task.sh b/scripts/codex_runner/run_codex_task.sh
index 5adaf1b1e685676ea7d89267afa7004229b4d6ff..4dbb8dac4f7b75eec7c51cd5e010123aa7ce9ec8
--- a/scripts/codex_runner/run_codex_task.sh
+++ b/scripts/codex_runner/run_codex_task.sh
@@ -72,7 +72,7 @@
 cd "$REPO_ROOT"
 
 ts="$(date +%Y-%m-%d_%H%M%S)"
-RUN_DIR="$REPO_ROOT/codex_runs/${ts}_${safe_name}"
+RUN_DIR="$REPO_ROOT/codex/runs/${ts}_${safe_name}"
 mkdir -p "$RUN_DIR"
 
 cp "$PROMPT_FILE" "$RUN_DIR/prompt.txt"

tokens used
13,378
