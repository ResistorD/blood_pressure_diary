# План доработки приложения Blood Pressure Diary

Этот документ описывает текущее состояние, выполненные улучшения и план дальнейших действий по приведению приложения в соответствие с дизайном Figma и улучшению качества кода.

## ✅ Выполнено

### 1. Главный экран (Home Screen)
- [x] **Карточка сводки (SummaryCard):**
    - Реализован дизайн с пульсирующим эффектом (Glow).
    - Добавлены декоративные элементы (иконка галочки, пульса).
    - Исправлено отображение времени и статуса.
    - Реализовано перекрытие (overlap) заголовка и карточки.
- [x] **Список записей (Record List):**
    - Обновлен дизайн карточек записей (скругления, тени).
    - Реализована группировка записей по дням ("Сегодня", "Вчера", даты).
    - Унифицирована логика цветов статуса (нормальное, повышенное и т.д.).
    - Заменен статус-индикатор с точки на вертикальную полосу (visual match).
- [x] **Навигация:**
    - Исправлена нижняя панель навигации.
    - Центральная кнопка "+" теперь использует правильную иконку `Plus.svg`.
    - Убрана лишняя вложенность `BlocProvider` в навигации.
- [x] **Пустое состояние:**
    - Добавлен дружелюбный экран "Нет записей" с призывом к действию.

### 2. Экран статистики (Statistics)
- [x] Заменена заглушка на реальные графики с использованием `fl_chart`.
- [x] Реализовано отображение систолического и диастолического давления.
- [x] Настроены цвета в соответствии с темой приложения (`AppUI`).
- [x] Добавлена легенда и форматирование осей.

### 3. База данных и Модели
- [x] Проанализированы модели Isar (`BloodPressureRecord`).
- [x] Убедились в наличии необходимых полей и индексов.

### 4. Темизация (AppTheme)
- [x] Скорректированы радиусы скругления (`cardRadius`) под Figma.
- [x] Проверены основные цвета (`primaryBlue`, `headerBlue`, `background`).

---

## 🚀 План дальнейших работ

### Приоритет 1: Функционал добавления (Add Record)
- [ ] **Валидация:** Проверить, что нельзя сохранить некорректные данные (например, 0/0).
- [ ] **UX:** Улучшить переход между полями ввода.
- [ ] **Дата и время:** Добавить возможность выбора даты/времени (сейчас сохраняется `DateTime.now()`).

### Приоритет 2: Настройки и Профиль
- [ ] **Настройки:**
    - Реализовать экспорт данных (CSV/PDF).
    - Добавить управление напоминаниями (local notifications).
    - Реализовать "Очистить данные" (сейчас заглушка).
- [ ] **Профиль:**
    - Сделать поля редактируемыми (возраст, рост, вес).
    - Сохранять данные профиля в `SharedPreferences` или Isar.

### Приоритет 3: Полировка UI/UX
- [ ] **Анимации:** Добавить плавные переходы при появлении элементов.
- [ ] **Dark Mode:** Адаптировать цвета для темной темы (сейчас дизайн оптимизирован под светлую).
- [ ] **Локализация:** Вынести строки в файлы локализации (сейчас хардкод на русском).

## Технические заметки
- Используется `flutter_bloc` для управления состоянием.
- `Isar` используется как локальная база данных.
- `GetIt` используется для внедрения зависимостей.
- `fl_chart` используется для графиков.
